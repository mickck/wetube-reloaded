extends ./base.pug
include mixins/video

block content
    form(method="GET")
        input(placeholder="Search by title", type="text",required,name="keyword")
        input(type="submit", value="Search")

    each video in videos
        +video(video) 